pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include(":extensions:en:manhwareader")

rootProject.name = "manhwa-reader-extensions"
